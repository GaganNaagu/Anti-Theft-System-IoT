---
phase: 02-lcd-integration-and-state-machine-controller
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [LCD_1inch3/LCD_1inch3.ino]
autonomous: true
requirements: [SYS-01, DISP-01, DISP-02]
must_haves:
  truths:
    - "LCD displays 'System Ready' splash screen on startup for 2 seconds"
    - "LCD updates to 'Vehicle Safe' exactly once when transitioning to safe state"
    - "LCD updates to red 'ALERT! Movement Detected' exactly once when transitioning to alert state"
    - "Display writes are strictly state-change triggered to avoid blocking the main loop"
  artifacts:
    - path: "LCD_1inch3/LCD_1inch3.ino"
      provides: "LCD initialization, splash screen, and transition-based screen drawings in state machine"
---

<objective>
Configure the Waveshare 1.3" SPI LCD to display the system states and implement state-change rendering logic to prevent display updates from blocking the main loop.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lcd-integration-and-state-machine-controller/02-CONTEXT.md
@LCD_1inch3/LCD_1inch3.ino
@LCD_1inch3/LCD_Driver.h
@LCD_1inch3/GUI_Paint.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add screen drawing functions and integrate STATE_SPLASH in state machine</name>
  <files>LCD_1inch3/LCD_1inch3.ino</files>
  <read_first>LCD_1inch3/LCD_1inch3.ino</read_first>
  <action>
    Modify `LCD_1inch3/LCD_1inch3.ino`:
    1. Define screen helper functions `drawSplashScreen()`, `drawSafeScreen()`, and `drawAlertScreen()` using standard `Paint_Clear()`, `Paint_DrawString_EN()`, and available font/color definitions.
    2. Add `STATE_SPLASH` to the `SystemState` enum.
    3. Initialize `currentState = STATE_SPLASH` and `splashStartTime = 0`.
    4. In `setup()`, call `drawSplashScreen()` and set `splashStartTime = millis()`. Remove the vendor test pattern drawing code.
    5. In `loop()`, if `currentState == STATE_SPLASH`, wait for 2 seconds (2000ms) non-blockingly using `millis() - splashStartTime >= 2000` before transitioning to `STATE_SAFE` and calling `drawSafeScreen()`.
    6. Update transition logic in `loop()` so `drawAlertScreen()` is called exactly once when transitioning to `STATE_ALERT`, and `drawSafeScreen()` is called exactly once when transitioning back to `STATE_SAFE`.
  </action>
  <verify>Verify sketch compiles successfully.</verify>
  <acceptance_criteria>
    - `LCD_1inch3.ino` contains `STATE_SPLASH` state
    - `setup()` triggers `drawSplashScreen()` and records `splashStartTime`
    - `loop()` has non-blocking 2-second splash screen check
    - `drawSafeScreen()` and `drawAlertScreen()` are called exactly once on state transitions
    - No compilation warnings/errors are raised
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Confirm no compilation warnings or errors are raised in `LCD_1inch3.ino`.
- Confirm display writes are restricted to transition blocks.
</verification>

<success_criteria>
- Plan completes compilation successfully.
- Screen draws only occur exactly once on state changes.
- Loop timing is fast and non-blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-lcd-integration-and-state-machine-controller/02-01-SUMMARY.md`
</output>
