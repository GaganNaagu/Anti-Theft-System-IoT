---
phase: 01-hardware-setup-and-sensor-interfacing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [LCD_1inch3/DEV_Config.h, LCD_1inch3/DEV_Config.cpp, LCD_1inch3/LCD_1inch3.ino]
autonomous: true
requirements: [HW-01, HW-02, HW-03, HW-04, SYS-02, ALRT-01, ALRT-02, ALRT-03, LOG-01]
must_haves:
  truths:
    - "Vibration sensor on D2 triggers alarm on HIGH"
    - "Tilt sensor on D3 triggers alarm on LOW"
    - "Active Buzzer on D6 and LED on D5 trigger instantly on sensor input"
    - "Alarm holds state for minimum 5 seconds"
    - "Buzzer and LED pulse at 1Hz frequency during active alarm state"
    - "Serial console prints alert messages"
  artifacts:
    - path: "LCD_1inch3/DEV_Config.h"
      provides: "Pin definitions for sensors and alarms"
      contains: "DEV_VIB_PIN"
    - path: "LCD_1inch3/DEV_Config.cpp"
      provides: "GPIO pin mode initialization for sensors and alarms"
    - path: "LCD_1inch3/LCD_1inch3.ino"
      provides: "Main polling, state machine and non-blocking timing loop"
---

<objective>
Configure the sensor inputs (SW-420 and SW-520D), alert outputs (Buzzer and LED), and implement the core non-blocking state machine with Serial logging.

Purpose: Establishes the primary physical and logical monitoring foundation before display rendering is added.
Output: Configured HAL definitions and state machine implementation in the main sketch.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hardware-setup-and-sensor-interfacing/01-CONTEXT.md
@.planning/phases/01-hardware-setup-and-sensor-interfacing/01-RESEARCH.md
@LCD_1inch3/DEV_Config.h
@LCD_1inch3/DEV_Config.cpp
@LCD_1inch3/LCD_1inch3.ino
</context>

<threat_model>
### Security Threat Model:
1. **Sensor Line Disconnection (Tampering)**:
   - Threat: An attacker cuts the signal lines of the sensors.
   - Analysis: For the SW-520D Tilt Sensor configured with `INPUT_PULLUP`, cutting the line floats the pin HIGH, which is interpreted as the "Upright/Safe" state.
   - Mitigation: In v1, this is documented as a physical vulnerability. In future milestones, a normally-closed loop configuration can be explored.
2. **Transducer Sabotage (Auditory Bypass)**:
   - Threat: Physically breaking the buzzer to silence the alarm.
   - Mitigation: Simultaneous dual alerting (both flashing LED and Serial logs) ensures visual and diagnostic traces remain functional.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add pin definitions and configure I/O modes in HAL</name>
  <files>LCD_1inch3/DEV_Config.h, LCD_1inch3/DEV_Config.cpp</files>
  <read_first>LCD_1inch3/DEV_Config.h, LCD_1inch3/DEV_Config.cpp</read_first>
  <action>
    1. Open `LCD_1inch3/DEV_Config.h` and add the following pin definitions right below `DEV_BL_PIN`:
       ```cpp
       #define DEV_VIB_PIN  2
       #define DEV_TILT_PIN 3
       #define DEV_LED_PIN  5
       #define DEV_BUZZ_PIN 6
       ```
    2. Open `LCD_1inch3/DEV_Config.cpp` and locate the `Config_Init` function.
    3. Add pin mode configuration at the end of `Config_Init`:
       ```cpp
       pinMode(DEV_VIB_PIN, INPUT);
       pinMode(DEV_TILT_PIN, INPUT_PULLUP);
       pinMode(DEV_LED_PIN, OUTPUT);
       pinMode(DEV_BUZZ_PIN, OUTPUT);
       
       // Ensure outputs are initialized to off
       DEV_Digital_Write(DEV_LED_PIN, 0);
       DEV_Digital_Write(DEV_BUZZ_PIN, 0);
       ```
  </action>
  <verify>Compile checks pass without errors.</verify>
  <acceptance_criteria>
    - `DEV_Config.h` contains `#define DEV_VIB_PIN  2`
    - `DEV_Config.h` contains `#define DEV_TILT_PIN 3`
    - `DEV_Config.h` contains `#define DEV_LED_PIN  5`
    - `DEV_Config.h` contains `#define DEV_BUZZ_PIN 6`
    - `DEV_Config.cpp` contains `pinMode(DEV_TILT_PIN, INPUT_PULLUP)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement polling, timing hold, and state machine in sketch</name>
  <files>LCD_1inch3/LCD_1inch3.ino</files>
  <read_first>LCD_1inch3/LCD_1inch3.ino, LCD_1inch3/DEV_Config.h</read_first>
  <action>
    Replace the contents of `LCD_1inch3/LCD_1inch3.ino` to implement the non-blocking state machine:
    - Maintain `setup()` display initializations but add `Serial.begin(9600);`.
    - Declare state variables: `enum AlarmState { STATE_SAFE, STATE_ALERT };` and `AlarmState currentState = STATE_SAFE;`.
    - Declare timing variables: `unsigned long alertStartTime = 0;` and `const unsigned long MIN_ALERT_HOLD_TIME = 5000;`.
    - Declare debounce variables for Tilt Sensor: `unsigned long tiltActiveStart = 0;` and `bool isTilted = false;`.
    - In `loop()`:
      1. Poll Vibration (`DEV_Digital_Read(DEV_VIB_PIN)`).
      2. Poll Tilt (`DEV_Digital_Read(DEV_TILT_PIN)`). If the pin is `LOW`, check if it remains `LOW` for 50ms using `millis() - tiltActiveStart >= 50` before setting `isTilted = true`. Reset `tiltActiveStart = millis()` if pin is `HIGH` (Safe).
      3. Handle State Transitions:
         - **SAFE -> ALERT**: If `currentState == STATE_SAFE` and (Vibration is `HIGH` or `isTilted` is `true`), set state to `STATE_ALERT`, set `alertStartTime = millis()`, write Buzzer and LED `HIGH`, and log `ALERT: Theft Attempt!` over Serial.
         - **ALERT -> SAFE**: If `currentState == STATE_ALERT` and `millis() - alertStartTime >= MIN_ALERT_HOLD_TIME` and Vibration is `LOW` and `isTilted` is `false`, set state to `STATE_SAFE`, write Buzzer and LED `LOW`, and log `Vehicle Safe` over Serial.
      4. Handle Flashing Alarm Outputs: If `currentState == STATE_ALERT`, toggle Buzzer and LED on/off every 500ms using a `millis()` pulse check to create a non-blocking 1Hz pulsing alert.
  </action>
  <verify>Verify sketch compiles successfully.</verify>
  <acceptance_criteria>
    - `LCD_1inch3.ino` contains `enum AlarmState`
    - `LCD_1inch3.ino` contains `Serial.begin(9600)`
    - `LCD_1inch3.ino` contains `MIN_ALERT_HOLD_TIME = 5000`
    - `LCD_1inch3.ino` contains `STATE_ALERT` and `STATE_SAFE` transition checks
    - No blocking `delay()` statements are introduced in `loop()`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify compilation success using available build tools.
- [ ] Confirm no compilation warnings or errors are raised in `DEV_Config` or `LCD_1inch3.ino`.
</verification>

<success_criteria>
- Plan completes compilation successfully.
- Digital outputs initialize to LOW on startup.
- Alert triggers instantly on input and is maintained for a minimum of 5 seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardware-setup-and-sensor-interfacing/01-01-SUMMARY.md`
</output>
