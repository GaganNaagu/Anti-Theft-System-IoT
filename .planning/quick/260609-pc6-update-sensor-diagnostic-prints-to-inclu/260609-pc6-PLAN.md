# Quick Plan 260609-pc6: update sensor diagnostic prints to include raw values

Update the print statements in `MODE_TEST_SENSORS` to output both the raw digital readings (0 or 1) and the descriptive status labels.

## Tasks
1. Update `LCD_1inch3.ino`'s sensor test print statements to print raw values.
