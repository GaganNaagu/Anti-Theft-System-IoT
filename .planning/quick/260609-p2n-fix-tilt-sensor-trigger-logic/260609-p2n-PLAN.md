# Quick Plan 260609-p2n: fix tilt sensor trigger logic

Invert the trigger logic of the tilt sensor (TILT_PIN) in `LCD_1inch3.ino` to active-HIGH, because upright is LOW (closed switch) and tilted is HIGH (open switch due to internal pull-up).

## Tasks
1. Update tilt sensor polling logic in `LCD_1inch3.ino` to use active-HIGH trigger.
2. Update sensor diagnostic test print statements in `LCD_1inch3.ino` to match the physical states.
